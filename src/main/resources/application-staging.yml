spring:
  datasource:
    url: jdbc:mysql://cockple-mysql:3306/cockple_staging?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul

  jpa:
    hibernate:
      ddl-auto: update

  data:
    redis:
      host: cockple-redis
      database: 1
